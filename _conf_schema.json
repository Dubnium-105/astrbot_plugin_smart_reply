{
    "enable": {
        "name": "启用",
        "type": "bool",
        "default": true,
        "description": "是否启用智能主动回复"
    },
    "prompt": {
        "name": "判断提示词",
        "type": "string",
        "default": "你是一个群聊中的 AI 助手。请判断是否应该回复这条消息。考虑因素：消息是否明确提到你、向你提问、或需要帮助；回复是否自然不突兀；如果只是群友闲聊不需要回复。只回复 YES 或 NO。",
        "description": "用于让 LLM 判断是否回复的提示词"
    },
    "history_count": {
        "name": "上下文条数",
        "type": "int",
        "default": 12,
        "description": "用于判断的最近聊天记录条数"
    },
    "provider_id": {
        "name": "模型ID",
        "type": "string",
        "default": "",
        "description": "用于判断的模型ID，留空使用默认模型"
    },
    "whitelist": {
        "name": "群聊白名单",
        "type": "list",
        "default": [],
        "description": "只在白名单群聊中启用，留空表示所有群聊"
    }
}
